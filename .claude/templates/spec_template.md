# [Feature] design

## Problem

[What's missing/broken today, why it matters. No solution yet.]

## [Core design decision — name it after the actual decision, e.g. "Resolution model"]

[The one decision everything else follows from. State it plainly, then justify.]

## Goals

- [Concrete, testable outcome]

## Non-goals

- [Explicitly out of scope — prevents scope creep during planning]

## API

[Public surface: new `Config`/`Generator` options, new classes, new `PropertyAttributeStrategy`/`Validator` contracts. Signatures, not prose.]

## Mechanism

[How it works internally — enough detail that the plan's tasks fall out of it directly.]

## Edge cases

- [Case] — [handling]

## Testing

[What must be covered: unit tests per class, `OfficialSchemaFixtureTest` if the public schema exercises this, `bin/check-fixture-drift.php` if coverage docs need a corresponding row.]
